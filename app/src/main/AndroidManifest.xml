<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tubes_kelg_c">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adapter.AdapterReview"></activity>
        <activity android:name=".Views.FragmentReview" />
        <activity android:name=".Views.TambahEditReview" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".splash.LoginActivity" />
        <activity android:name=".geolocation.PlaceActivity" />
        <activity android:name=".splash.CheckActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".splash.SplashActivity" />
        <activity android:name=".camera.Camera" />
        <activity android:name=".DaftarBooking" />
        <activity android:name=".SewaMotor" />
        <activity android:name=".RecyclerViewListMotor" />
        <activity android:name=".MainActivity" />

        <service android:name=".notification.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>